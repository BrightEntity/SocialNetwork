@using Microsoft.AspNet.Identity
@using SocialNetwork.Models
@if (Request.IsAuthenticated)
{
    <div class="navbar-right">
        <ul class="nav navbar-nav">
            <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span></a>
            <ul class="dropdown-menu" id="friends-dropdown">
                
            </ul></li>
            <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-envelope"></span></a>
            <ul class="dropdown-menu" id="messages-dropdown">
                <li>Texte <a href>Lu</a></li>
            </ul>
            </li>
            <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-globe"></span></a>
            <ul class="dropdown-menu" id="notifications-dropdown">
                
            </ul></li>

            <li>
                @Html.ActionLink(User.GetApplicationUser().SocialProfile.Display, "Index", "Profile", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Gérer le compte", "Index", "Manage")</li>
            </ul>
            </li>


        </ul>

        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "LogOffForm", @class = "navbar-form", style = "display: inline" }))
        {
        @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-danger navbar-btn"><span class="glyphicon glyphicon-off"></span></button>
        }
    </div>
        
}
else
{
    <ul class="nav navbar-form navbar-right">
        @Html.Partial("_LoginInline")
    </ul>
}
